# Ajustes en modelo de permisos

```php
//  modelo Roles, Permisos y Módulos

use HasUuids;
use SoftDeletes;


protected $keyType = 'string';
protected $primaryKey = 'id';
public $incrementing = false;

```
